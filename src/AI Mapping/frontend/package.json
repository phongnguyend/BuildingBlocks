{
  "name": "ai-mapping-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "build:mobile": "vite build --mode mobile",
    "cap": "cap",
    "mobile:sync:android": "npm run build:mobile && cap sync android",
    "mobile:build:android": "npm run mobile:sync:android && cd android && gradlew.bat assembleDebug",
    "mobile:open:android": "npm run mobile:sync:android && cap open android",
    "mobile:run:android": "npm run mobile:sync:android && cap run android",
    "mobile:doctor:android": "cap doctor android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0",
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
